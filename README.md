# Plya Med Landing

Independent React/Vite landing page concept for Plya Med peptide strips.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## GitHub Pages

Pushing to `main` runs `.github/workflows/deploy.yml` and deploys the Vite build from `dist`.
